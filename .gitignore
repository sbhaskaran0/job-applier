# Secrets — never commit
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Batch-apply queue prep files/sheets (transient, per-run)
data/prep/

# Per-job tailored resumes + cover letters (personal artifacts, like data/)
resumes/

# Local postings store + digest (cache of public ATS data; rebuilt by
# `python -m src.refresh` — only first_seen history is lost on delete)
data/postings.db*
data/digest-latest.md

# Transient browser artifacts / screenshots
/*.png
current_page.png
*_review.png

# OS
.DS_Store
Thumbs.db
